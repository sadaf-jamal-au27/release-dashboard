name: infra-plan

on:
  pull_request:
    paths:
      - "infra/**"
      - ".github/workflows/10_infra_plan.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Terraform plan would run here (placeholder)."

